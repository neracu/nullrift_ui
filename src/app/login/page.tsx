import LoginCardSection from "@/components/login-signup";

/**
 * Marketing gate: collect email/password locally, then continue to the Builder (no auth API).
 */
export default function LoginPage() {
  return <LoginCardSection />;
}

// Made with Bob
