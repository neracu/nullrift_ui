import { BuilderClient } from "./builder-client";

/**
 * App Router page for /builder — main AI component generation UI.
 */
export default function BuilderPage() {
  return <BuilderClient />;
}

// Made with Bob
